# Google AI API Key

GOOGLE_API_KEY="your_google_api_key"

# Gemini Model (Optional)

# Defaults to gemini-2.5-pro if not set (free for students in 2025)

GEMINI_MODEL="gemini-2.5-pro"

# --- Target Database Configuration ---

# This is the database where the combined schema will be created.

# It can be the local Docker container or an external database.

DBTARGET_NAME="dbTarget"
DBTARGET_USER="user"
DBTARGET_PASSWORD="password"

# Example Connection URL for an external database:

# DBTARGET_URL="postgresql://user:password@host:port/dbTarget"
