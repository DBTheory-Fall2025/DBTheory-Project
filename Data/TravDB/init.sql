CREATE TABLE "Travel1" (
    "TravelOptions" VARCHAR(255) PRIMARY KEY,
    "dist" INTEGER,
    "speed" INTEGER
);

CREATE TABLE "addr1" (
    "ID" VARCHAR(255) PRIMARY KEY,
    "address" INTEGER,
    "income" NUMERIC
);
